Global Multi-Asset Scanner V5.10.0 — Package 74

This package consolidates the V5.10 discovery, ranking and Telegram anti-noise fixes
directly into the scanner runtime. The GitHub workflow no longer mutates source code
during a run.

LIVE remains disabled.

Before installing, replace the repository files from this package. If GitHub Actions
still shows a failure with 0 steps / runner_id 0, that is a GitHub-hosted runner
startup failure occurring before checkout; it is not caused by the Python scanner.
